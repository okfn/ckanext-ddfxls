[project]
name = "ckanext-ddfxls"
version = "0.1.2"
description = "A datastore dump format for XLS files"
readme = "README.md"
authors = [
    {name = "OKFN", email = "info@okfn.org"}
]
license = "AGPL-3.0-or-later"
license-files = ["LICENSE"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

keywords = ["CKAN", "excel", "xls", "datastore", "dump", "xlsx"]
dependencies = [
    # use requirements.txt file
    # "openpyxl>=3,<4",
]

[project.optional-dependencies]
dev = [
    "pytest-ckan",
]

[project.urls]
Homepage = "https://github.com/okfn/ckanext-ddfxls"

[project.entry-points."ckan.plugins"]
ddfxls = "ckanext.ddfxls.plugin:DdfxlsPlugin"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["ckanext*"]
namespaces = true

[tool.setuptools.package-data]
"ckanext.ddfxls" = [
    "assets/*.js",
    "assets/*.css",
    "assets/*.yml",
    "templates/**/*.html",
    "public/**/*",
]

[tool.pytest.ini_options]
filterwarnings = [
    # Silence third-party deprecation noise but keep SQLAlchemy warnings
    # visible: they signal the SA 2.0 migration breaks.
    # Later entries take precedence over earlier ones.
    "ignore::DeprecationWarning",
    "default::sqlalchemy.exc.SADeprecationWarning",
    "default::sqlalchemy.exc.SAWarning",
]
addopts = "--ckan-ini test.ini"

[tool.ruff]
line-length = 127

[tool.ruff.lint]
select = ["E", "F", "W"]
